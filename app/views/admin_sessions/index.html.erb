<h1>Listing admin_sessions</h1>

<table>
  <tr>
    <th>Username</th>
    <th>Password</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @admin_sessions.each do |admin_session| %>
  <tr>
    <td><%= admin_session.username %></td>
    <td><%= admin_session.password %></td>
    <td><%= link_to 'Show', admin_session %></td>
    <td><%= link_to 'Edit', edit_admin_session_path(admin_session) %></td>
    <td><%= link_to 'Destroy', admin_session, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Admin session', new_admin_session_path %>
