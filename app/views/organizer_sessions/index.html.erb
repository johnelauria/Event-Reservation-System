<h1>Listing organizer_sessions</h1>

<table>
  <tr>
    <th>Username</th>
    <th>Password</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @organizer_sessions.each do |organizer_session| %>
  <tr>
    <td><%= organizer_session.username %></td>
    <td><%= organizer_session.password %></td>
    <td><%= link_to 'Show', organizer_session %></td>
    <td><%= link_to 'Edit', edit_organizer_session_path(organizer_session) %></td>
    <td><%= link_to 'Destroy', organizer_session, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Organizer session', new_organizer_session_path %>
