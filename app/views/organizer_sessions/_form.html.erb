<%= form_for(@organizer_session) do |f| %>
  <% if @organizer_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@organizer_session.errors.count, "error") %> prohibited this organizer_session from being saved:</h2>

      <ul>
      <% @organizer_session.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :username %><br />
    <%= f.text_field :username %>
  </div>
  <div class="field">
    <%= f.label :password %><br />
    <%= f.password_field :password %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
